[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "budget-inspector"
version = "0.1.0"
description = "An evidence-first agent & analytical toolkit for inspecting Philippine national budget data."
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "Vibe Coders Team", email = "kaarlan@up.edu.ph" }
]
dependencies = [
  "duckdb>=1.0.0",
  "pandas>=2.0.0",
  "openpyxl>=3.1.0",
  "pyarrow>=15.0.0",
  "rapidfuzz>=3.0.0",
  "pydantic>=2.0.0",
  "typer>=0.9.0",
  "requests>=2.30.0",
  "beautifulsoup4>=4.12.0",
  "tabulate>=0.9.0",
  "rich>=13.0.0"
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0"
]

[project.scripts]
budget-inspector = "budget_inspector.cli:app"

[tool.setuptools.packages.find]
where = ["src"]
